import subprocess
import schedule
import time

# def run_custom_command():
#
#     command = 'python manage.py sync_all_apps'
#     try:
#         # Run the custom command using subprocess
#         result = subprocess.run(command, shell=True, text=True, capture_output=True, cwd='..')  # Use '..' to refer to the parent directory
#         if result.returncode == 0:
#             print("Command executed successfully.")
#             print(result.stdout)
#         else:
#             print("Command execution failed.")
#             print(result.stderr)
#     except Exception as e:
#         print(f"An error occurred: {e}")
#
# # Schedule the custom command to run every 10 seconds
# schedule.every(5).seconds.do(run_custom_command)
#
# while True:
#     schedule.run_pending()
#     time.sleep(1)


while True:
    pass
